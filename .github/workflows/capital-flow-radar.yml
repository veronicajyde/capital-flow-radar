name: Capital Flow Radar

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write

jobs:
  radar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Test Python
        shell: python
        run: |
          print("================================")
          print("CAPITAL FLOW RADAR")
          print("Python execution successful")
          print("================================")
